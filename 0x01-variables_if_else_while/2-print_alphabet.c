#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - print if the number is positive 
 *
 *Description: using the main function 
 * this program prints "programming is the best
 * Return: 0
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
